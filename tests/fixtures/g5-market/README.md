# G5 Market Contract Fixtures

Generated by the corrected G5 HTTP acceptance run `189b6` on 2026-09-15,
using an independent SQLite store and test keys under
`D:/phpstudy_pro/g5-local/codex-20260915/`.

These packages and the public key are test-only. No private key, real member
data, production key or live download token is included. Tokens in the JSON
catalogs are redacted; the client test replaces only the download URL with a
syntactic token URL and injects an offline transport. That verifies client
validation and installation, not live grant authorization or quota.

Package bytes, hashes and signatures are unchanged from the HTTP output.
The CMS integration tests run in separate PHP processes as the planned
1.20.0 release, without modifying config/version.php or LICENSE_PUBKEY_B64.

The original Claude sample remains at `D:/phpstudy_pro/g5-local/samples/`.
Its template used section.children, which the CMS normalized into an empty
section. The corrected sample uses sections[].columns[].elements[].data.
