# 易登录 1.0.0 验证记录

验证日期：2026-09-14。开发交付目录：`D:\phpstudy_pro\WWW\yikaicms.codex.yikai`。

以该目录当时的已提交代码及未提交更新为测试基线。浏览器与 HTTP 冒烟使用隔离副本和新建 SQLite 测试库；没有修改开发站数据库、启用开发站插件或对外发布。

## 已通过

以下是开发基线的既有验证记录，不代表 v1.20.0 正式安装包已验收。

- PHP 8.2.9：新增 PHP 文件及登录入口语法检查。
- `php vendor/bin/phpunit --filter 'DoLoginLink|Migrator|InstallSqlMigrationParity'`：19 个测试，453 个断言通过。
- 新增 `tests/smoke/dologin.php`：32 个 HTTP 检查通过，包含插件启停、管理权限、CSRF、普通登录、一次性兑换、撤销、过期、账号停用、IP 白名单、限流、TOTP 待验证与完成、日志无原始 token。
- `tests/smoke/admin_pages.php`：97 页无 500，16 个核心页面均为 200。
- `tools/check_lang_keys.php` 与 `tools/check_blox_i18n.php` 均通过；插件自带中英日三语。
- 内置浏览器验证桌面管理页、390px 窄屏管理页与确认登录页；fragment 在页面加载后从地址栏移除，确认按钮启用，浏览器没有 error/warn 日志。
- SQLite 迁移重复执行及全新安装 SQL 与迁移的一致性通过。MySQL SQL 使用 5.7 兼容语法；本次没有在 MySQL 5.7 实例上执行。

## 整个工作区仍未通过的检查

- 全量 PHPUnit：1928 个测试，16655 个断言，1 个错误、7 个失败，涉及 Blox 模板目录、预览、元素库、前台预览、主题 Banner 校验和编辑器源码大小。新增迁移引起的新装 SQL 不一致已修复并回归通过。
- 全量 Psalm：排除 `config/config.php` 后仍有 66 个 ERROR，主要位于 BuilderRegistry、Blox 页面和元素；本次新增模型及修改的登录入口未出现在错误清单中。
- 因此这份记录只证明本插件专项检查通过，不能作为整个 CMS 合并或发版门禁通过的依据。

原始全量结果保存在开发目录的 `storage/temp/dologin-phpunit-final.txt`、`storage/temp/dologin-psalm.json`，不随插件交付。

## 交付范围

插件目录、登录扩展钩子、模型与工厂、独立迁移、新装 MySQL/SQLite 表定义及专项测试。没有修改原有 Blox 实现或全局语言文件。新增样式均为插件自带 CSS，没有新增 Tailwind 工具类。

插件默认不启用。使用方式见同目录 `README.md`。临时链接不是临时会话：过期或撤销不会自动踢出已登录用户。

## v1.20.0 上架目标

2026-09-14 按用户决定，下一次面向大规模推广的 CMS 版本为 v1.20.0。本插件最低 CMS 版本同步为 1.20.0，插件自身版本仍为 1.0.0；不实施 v1.19.9 兼容分支。

- [ ] 确认 v1.20.0 安装包与升级包均交付登录钩子、模型与工厂、迁移；全新安装 SQL 包含对应表结构。
- [ ] 在实际 v1.20.0 候选包上验证插件安装、初始化、签发/兑换/撤销、双重验证和停用行为。
- [ ] 确认市场元数据与 ZIP 内 requires_cms 一致；旧版安装器可能不强制检查此字段，最低版本声明不能代替实际拦截与明确提示。
- [ ] 完成发布门禁并取得上架授权后，才签名和发布插件包。

本次只修订兼容性声明和发布目标，没有重新执行上述候选包验收，也没有上架。

## 后续授权上架记录

2026-09-14 用户随后明确授权提前上架，仅限 CMS v1.20.0 及以上使用；不要求 v1.19.9 兼容。上段“没有上架”描述的是此前修订阶段，当前插件已发布。

- 增加插件自身的版本检查，最低版本读取 plugin.json。旧站不注册免密登录钩子；管理页和登录处理在访问模型前返回升级提示。PHP 缺少 CMS_VERSION、非法版本、1.19.9、1.19.10、1.20.0-rc1 均拒绝；1.20.0、1.20.1、2.0.0 通过版本判断。
- 本次独立运行 `DoLoginCompatibilityTest|DoLoginLinkTest`：11 tests、89 assertions 通过。版本入口测试使用独立 PHP 进程及测试替身，未修改真实版本号、站点授权或账号。
- 插件 PHP 语法通过。原先完整 HTTP 功能验证仍是上述开发基线记录，本次未重新执行 v1.20.0 正式候选包验收。
- 仅发布 `packages/plugins/dologin-v1.0.0.zip` 与 `data/plugins.json`。线上目录原有 10 个插件记录保持不变，新增易登录 1.0.0，免费，requires_cms=1.20.0。
- ZIP 明确包含 12 个插件文件，不包含核心文件、测试、凭据和本验证报告。RSA-SHA256 签名经 CMS 公钥验证；FTPS 上传两文件均完成回读 SHA256 校验，公开 HTTPS 下载再次校验一致。
- ZIP SHA256：`c3b36796d043add7ffdfb182fff1278f5bc942072bd11c9e1904440d9d86f73b`。
- 备份、签名与上传记录：`D:/phpstudy_pro/WWW/yikaicms-docs/codex-updates/dologin-1.0.0-20260914/`。

已上架插件不等于已发布 CMS v1.20.0。该核心版本的安装/升级包依赖交付与候选包验证仍须完成。旧安装器可能允许解压本包，但插件自身拒绝在低版本上使用；本次未修改全站插件安装器。
